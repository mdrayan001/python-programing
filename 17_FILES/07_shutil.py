import shutil

# Removes the directory named "dir" and all its contents (files and subdirectories)
shutil.rmtree("dir")  
shutil.copy("rayan.text","rayan malik.txt")

shutil.move("rayan malik.txt","dir/")
