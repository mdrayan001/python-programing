# tuples are immutable and ordered collections of items
# they can contain mixed data types
# tuples are defined using parentheses

a = (1, 2, 3)
print(a)
print(a[2])

# a[2] = 4  This will raise an error because tuples are immutable
# single element tuple
b = (1,)

